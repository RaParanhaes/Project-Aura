# Requests for Comments

RFCs are for substantial designs that are still being evaluated. They are proposals, not project truth.

Use an RFC when a future feature has meaningful alternatives/trade-offs (for example memory architecture or social engine design). Once a decision is accepted, record the durable result in an ADR and link back to the RFC.
