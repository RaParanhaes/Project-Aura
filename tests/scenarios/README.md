# Scenario Tests

End-to-end Golden Scenarios that prove user-visible AURA behavior.

Examples:
- login → enter room → walk → chat
- disconnect → reconnect
- two headless users sharing a room

Scenario IDs should stay stable (`GS-001`, `GS-002`, ...).
