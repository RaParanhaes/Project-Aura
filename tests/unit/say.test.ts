import { describe, expect, it } from 'vitest';
import { CapabilityRegistry, registerSay } from '../../packages/runtime/src/index.ts';
describe('SAY capability', () => { it('accepts room speech', async () => { const registry = new CapabilityRegistry(); const calls: unknown[] = []; registerSay(registry, async (...args) => { calls.push(args); }); await expect(registry.request('SAY', { text: 'oi' }, { world: { roomId: 1 } }, 'say-1')).resolves.toEqual({ actionId: 'say-1', status: 'pending' }); expect(calls).toHaveLength(1); }); it('rejects invalid or roomless speech', async () => { const registry = new CapabilityRegistry(); registerSay(registry, async () => undefined); await expect(registry.request('SAY', { text: '' }, { world: { roomId: 1 } }, 'bad')).resolves.toMatchObject({ status: 'rejected' }); await expect(registry.request('SAY', { text: 'oi' }, { world: {} }, 'no-room')).resolves.toMatchObject({ status: 'rejected', reason: 'room observation required' }); }); });
