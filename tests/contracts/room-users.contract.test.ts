import { describe, expect, it } from 'vitest';
import { PacketWriter, parseRoomUsers } from '../../packages/protocol/src/index.js';
const habbo = (id: number, name: string) => new PacketWriter().writeInt(id).writeString(name).writeString('motto').writeInt(0).writeInt(0).writeInt(0).writeInt(0).writeString('hd-180-1').writeInt(id + 100).writeInt(4).writeInt(7).writeString('0').writeInt(2).writeInt(1).writeString('M').writeInt(-1).writeInt(-1).writeString('').writeString('').writeInt(0).writeBoolean(true).writeString('').writeString('').writeString('').writeString('').writeString('').writeString('').writeString('').writeString('unknown').writeInt(0).writeInt(0);
describe('RoomUsers contract', () => { it('decodes Cabana and the test account from the roster', () => { const body = new PacketWriter().writeInt(2).writeBytes(habbo(1, 'Cabana').toUint8Array()).writeBytes(habbo(2, 'octane_test_1_mt').toUint8Array()).toUint8Array(); expect(parseRoomUsers(body)).toMatchObject([{ id: 1, name: 'Cabana' }, { id: 2, name: 'octane_test_1_mt' }]); }); });
