# Integration Tests

Validate boundaries working together.

Examples:
- `RealSession` against Polaris
- persistence adapter against its database
- protocol decoder feeding WorldState

Keep environment requirements explicit so these tests are never confused with fast unit checks.
