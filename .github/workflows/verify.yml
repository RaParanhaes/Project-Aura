name: AURA Verify

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  foundation:
    name: Foundation checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.25.0 --activate

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Capture generated lockfile
        run: |
          echo '=== AURA_LOCKFILE_BEGIN ==='
          cat pnpm-lock.yaml
          echo '=== AURA_LOCKFILE_END ==='

      - name: Doctor
        run: pnpm run doctor

      - name: Verify foundation
        run: pnpm run verify
